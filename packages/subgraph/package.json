{
  "name": "@nuru/subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Nuru Voice Payment System on BASE",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nuru-voice-payments",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nuru-voice-payments",
    "create-local": "graph create --node http://localhost:8020/ nuru-voice-payments",
    "remove-local": "graph remove --node http://localhost:8020/ nuru-voice-payments",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.65.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
